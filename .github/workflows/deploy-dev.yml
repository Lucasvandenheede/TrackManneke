name: Deploy TrackManneke Dev

on:
  push:
    branches:
      - dev

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Prepare repository on VM
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.GCP_VM_IP }}
          username: ${{ secrets.GCP_SSH_USER }}
          key: ${{ secrets.GCP_SSH_PRIVATE_KEY }}
          script: |
            mkdir -p ~/trackmanneke-dev
            cd ~/trackmanneke-dev
            if [ ! -d ".git" ]; then
              git clone https://github.com/${{ github.repository }}.git .
            fi
            git fetch --all
            git checkout ${{ github.sha }}

      - name: Write .env file
        run: |
          cat > .env << 'EOF'
          DISCORD_TOKEN=${{ secrets.DEV_DISCORD_TOKEN }}
          GUILD_ID=${{ secrets.DEV_GUILD_ID }}
          ENVIRONMENT=development
          TOTD_CHANNEL_ID=${{ secrets.DEV_TOTD_CHANNEL_ID }}
          COTD_CHANNEL_ID=${{ secrets.DEV_COTD_CHANNEL_ID }}
          USER_AGENT=${{ secrets.USER_AGENT }}
          NADEO_SERVICE_ACCOUNT_LOGIN=${{ secrets.NADEO_SERVICE_ACCOUNT_LOGIN }}
          NADEO_SERVICE_ACCOUNT_PASSWORD=${{ secrets.NADEO_SERVICE_ACCOUNT_PASSWORD }}
          OAUTH_CLIENT_ID=${{ secrets.OAUTH_CLIENT_ID }}
          OAUTH_CLIENT_SECRET=${{ secrets.OAUTH_CLIENT_SECRET }}
          TIMEZONE=${{ secrets.TIMEZONE }}
          EOF

      - name: Copy .env to VM
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.GCP_VM_IP }}
          username: ${{ secrets.GCP_SSH_USER }}
          key: ${{ secrets.GCP_SSH_PRIVATE_KEY }}
          source: ".env"
          target: "~/trackmanneke-dev/"
          strip_components: 0

      - name: Build and deploy container
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.GCP_VM_IP }}
          username: ${{ secrets.GCP_SSH_USER }}
          key: ${{ secrets.GCP_SSH_PRIVATE_KEY }}
          script: |
            cd ~/trackmanneke-dev

            docker build -t trackmanneke-dev .

            docker stop trackmanneke-dev || true
            docker rm trackmanneke-dev || true

            docker run -d \
              --name trackmanneke-dev \
              --restart unless-stopped \
              --env-file $(pwd)/.env \
              -v trackmanneke-dev-data:/app/src/db \
              trackmanneke-dev
